name: Repository Tree

on:
  workflow_dispatch:

jobs:
  tree:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install tree
        run: sudo apt-get update && sudo apt-get install -y tree

      - name: Print repository tree
        run: tree -a -I '.git|.gradle|build'
